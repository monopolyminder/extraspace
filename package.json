{
  "name": "extraspacescam",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "serve": "python3 -m http.server 8000 --directory dist"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  },
  "dependencies": {
    "turndown": "^7.2.4"
  }
}
